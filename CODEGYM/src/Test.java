import java.io.*;

public class Test {
    public static void main(String[] args) throws IOException {
        /*InputStream inputStream=System.in;
        Reader inputStreamReader=new InputStreamReader(inputStream);
        BufferedReader bufferedReader=new BufferedReader(inputStreamReader);

        String name=bufferedReader.readLine();  //  read a string rom the keyboard
        String sAge= bufferedReader.readLine(); //  read a string from the keyboard
        int nAge=Integer.parseInt(sAge);    //  convert thhe string to a number*/
        /*BufferedReader reader=new BufferedReader(new InputStreamReader(System.in));
        String name1=reader.readLine();
        String name2=reader.readLine();
        String name3=reader.readLine();
        System.out.println(name1+" "+name2+" "+name3+" = Pure love. Ooo la-la");*/
        "hello".length();

    }
}
